apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: my-postgresql
  namespace: default
spec:
  instances: 2
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: todos
      owner: anish
      secret:
        name: postgresql-credentials